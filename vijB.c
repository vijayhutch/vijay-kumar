#include <stdio.h>
#include <string.h>
int main()
{
char s[100];
int count=0,i;
clrscr();
printf("\n Enter the sentence:");
scanf("%s",s);
for(i=0;s[i]!=';i++)
{
if(s[i]= ')
count++;    
}
printf("\n Number of words are: %d",count+1);
getch();
return 0;
}
