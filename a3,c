

#include<stdio.h>
int main(void)
{
	int i,a,largest,b[3],smallest;
	scanf("%d",&a);
	for(i=0;i<3;i++)
	scanf("%d",&b[i]);
	largest=b[0];
	for(i=0;i<n;i++)
	if(b[i]>largest)
	largest=b[i];
	printf("%d is the largest\n",largest);
	smallest=b[0];
	for(i=0;i<n;i++)
	if(b[i]<smallest)
  smallest=b[i];
	printf("%d is the smallest",smallest);
	return 0;
}
