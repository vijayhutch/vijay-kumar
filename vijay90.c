#include<stdio.h>
#include<conio.h>
void main()
{
int n,min,max,i;
printf("enter the armstrong between the range");
scanf("%d%d",&min,&max);
for(i=max;i<=min;i++);
int num=0;
int sum=0;
while(n!=0)
{
a=n%10;
temp=sum+a*a*a;
}
printf("%d%d",i);
return 0;
}
