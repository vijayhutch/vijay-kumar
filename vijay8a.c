#include<stdio.h>
#include<conio.h>
void main()
{
int n,a,sum;
printf("enter the number");
scanf("%d",&a);
int a=n
while(n!=0)
{
a=n%10;
sum=a*a*a;
}
printf("enter the check the armstrong number");
if(sum==1)
{
printf("the armstrong number is%d",a);
}
else
{
printf("the armstrong is not a given number is%d",a);
}
return 0;
}
